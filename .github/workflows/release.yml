name: Release

on:
  push:
    tags:
      - "v*"

# Minimum required to create the release and upload assets; avoids inheriting org default read-write
permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if tag is on master branch
        run: |
          if [[ "${{ github.ref_type }}" != "tag" ]]; then
            echo "Not a tag, skipping release"
            exit 1
          fi

          TAG_COMMIT=$(git rev-list -n 1 ${{ github.ref_name }})

          if ! git branch -r --contains $TAG_COMMIT | grep -q "origin/master"; then
            echo "Tag ${{ github.ref_name }} is not on master branch, skipping release"
            exit 1
          fi

          echo "Tag ${{ github.ref_name }} is on master branch, proceeding with release"

      - name: Get version from tag
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          VERSION=${VERSION#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Set version file
        run: |
          echo "${{ steps.version.outputs.version }}" > cmd/version.txt

      - name: Build binaries for all platforms
        run: |
          make build-all

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Release ${{ steps.version.outputs.tag }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            bin/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
